

di ; An instruction that takes no operands

cp a, $11 ; Compare value left by boot ROM, an instruction that takes TWO operands

jr .local ; An instruction that takes one OR two operands (in this case, it is taking one)